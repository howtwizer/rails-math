<p id="notice"><%= notice %></p>

<h1>Tests</h1>

<table>
  <thead>
    <tr>
      <th>Число 1</th>
      <th></th>
      <th>Число 2</th>
      <th>Ответ</th>
      <th>Правильно?</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tests.each do |test| %>
      <tr>
        <td><%= test.a %></td>
        <td><%= test.sign %></td>
        <td><%= test.b %></td>
        <td><%= test.result %></td>
        <td><%= test.string_result %></td>
<!--        <td><%#= link_to 'Show', test %></td>-->
<!--        <td><%#= link_to 'Edit', edit_test_path(test) %></td>-->
<!--        <td><%#= link_to 'Destroy', test, method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
<!--        <td><%#= link_to 'Destroy', test, method: :delete %></td>-->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<p> Всего: <%= @tests.count %> </p>
<p> Верно: <%= @tests.where(right: true).count %></p>
<p> Неверно: <%= @tests.where(right: false).count %></p>

<%= link_to 'Решать примеры!', new_test_path %>
<%= link_to 'Очистить результаты', destroy_all_tests_path, method: :delete %>
